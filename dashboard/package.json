{
  "name": "aggregator-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.tsx --bundle --outfile=dist/assets/app.js --tsconfig=tsconfig.json",
    "postbuild": "mkdir -p dist && cp -r public/* dist/"
  },
  "devDependencies": {
    "esbuild": "0.23.1",
    "typescript": "5.5.4",
    "@types/react": "18.3.3",
    "@types/react-dom": "18.3.0"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  }
}
